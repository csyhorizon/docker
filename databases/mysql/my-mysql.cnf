[client]
default-character-set=utf8mb4

[mysql]
default-character-set=utf8mb4

[mysqld]
# 한글 및 이모지 완벽 지원을 위한 설정
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

# 인증 방식 설정 (최신 라이브러리와의 호환성을 위해)
authentication_policy=mysql_native_password

# 최대 접속 연결 수 (필요시 조정)
max_connections=100